[project]
name = "koralogging"
version = "0.1.0"
description = "A high-performance logging SDK for microservices."
readme = "README.md"
authors = [{name = "PerfectTeaBR"}]
dependencies = [
    "asyncio"   
]

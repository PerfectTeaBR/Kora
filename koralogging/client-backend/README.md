# 🖥️ ClientBackend
Aqui vai um guia completo de como iniciar o ClientBackend com o Kora!

```python
from koralogging import clientbackend

client = kora.client.backend

client.backend.initialize(
  backend.start()
 on_ready = lambda: logging.info("O Seu Client Backend foi iniciado.")
)
)
```